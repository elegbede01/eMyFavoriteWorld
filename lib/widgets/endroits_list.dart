import 'package:flutter/material.dart';

class EndroitsList extends StatelessWidget {
  const EndroitsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
